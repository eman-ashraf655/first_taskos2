name: Test Workflow

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 'Checkout code'
        uses: actions/checkout@v2

      - name: 'Login to Docker Hub'
        run: docker login --username eman255 --password dckr_pat_2tKVa_VxYHHuwi_109FOQM3nKtU
